<h1>User</h1>

<div>
    <%= @user.email %> <br>
    <%= link_to 'Edit User', user_edit_path(@user) %> <br>
    <%= button_to 'Delete User', user_destroy_path(@user), method: :delete, form: { data: { turbo_confirm: 'Are you sure?' } } %>
</div>
